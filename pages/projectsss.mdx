import BlogPage from '@/components/BlogPage';
import Sections from '@/components/Sections';

export default function BlogPost({ children }) {
  const meta = {
    title: 'Projects',
    description:
      "Check the current projects I've been working on. They range from programming related ones to gaming and more.",
    date: '2021-07-27T00:00:00.000Z',
    categories: [],
    feature: false,
  };
  return <BlogPage meta={meta}>{children}</BlogPage>;
}
